{
  "name": "aibridge-extension",
  "displayName": "AiBridge",
  "description": "HTTP API for external applications to inject text into VS Code, Cursor, Windsurf, and other VS Code-based editors",
  "publisher": "mobai",
  "version": "1.1.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MobAI-App/aibridge-extension"
  },
  "homepage": "https://github.com/MobAI-App/aibridge-extension#readme",
  "bugs": {
    "url": "https://github.com/MobAI-App/aibridge-extension/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "categories": ["Other"],
  "keywords": ["ai", "chat", "injection", "http", "api", "cursor", "windsurf", "copilot", "automation"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "AiBridge",
      "properties": {
        "aibridge.port": {
          "type": "number",
          "default": 9999,
          "description": "Port for the HTTP server"
        },
        "aibridge.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host for the HTTP server"
        },
        "aibridge.paranoid": {
          "type": "boolean",
          "default": false,
          "description": "Inject without auto-submit"
        },
        "aibridge.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start server on extension activation"
        }
      }
    },
    "commands": [
      {
        "command": "aibridge.start",
        "title": "AiBridge: Start Server"
      },
      {
        "command": "aibridge.stop",
        "title": "AiBridge: Stop Server"
      },
      {
        "command": "aibridge.status",
        "title": "AiBridge: Show Status"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
